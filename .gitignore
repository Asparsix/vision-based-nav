# build artifacts
build/
install/
log/
__pycache__/
*.pyc
*.swp
*.swo
*~

# local envs / overlays (too large for GitHub)
.venv/
venv/
.opt

# keep useful data/, but skip bulky duplicates
data/unet_runs/
data/maps/*.back
data/maps/*.db.back
data/preview_*.png

# do NOT ignore all png/jpg/pt/db globally — sim textures + models need them
