% This file is superseded by the single document:
%   ARCHITECTURE_OVERLAY.tex
% which now contains the flowchart + Needs/Provides/Feeds contracts + plan.
% Compile that file instead:
%   pdflatex ARCHITECTURE_OVERLAY.tex
\documentclass{article}
\begin{document}
See \texttt{ARCHITECTURE\_OVERLAY.tex} (single architecture overlay document).
\end{document}
